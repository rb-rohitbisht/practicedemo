// now learn pow method 
// math.pow(a,b) gives value a to the power of b 

const power = function( num1,num2){

 let pow = Math.pow(num1,num2);
 console.log(`${num1} to the power of ${num2} = ` + pow);
}
console.log(power(5,5));
