// now learn math.sqrt method 
// this is used to give a square root of a given number
 const sqrtNum = function(num){
     let squareRoot = Math.sqrt(num);
     console.log(` square root of a number ${num} = `+ squareRoot);
 }
 console.log(sqrtNum(64));